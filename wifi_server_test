from flask import Flask, request

app = Flask(__name__)

@app.route('/test')
def test():
    data = request.args.get('data')
    print(f" QMC value: {data}")
    return "Data received", 200

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5001)
